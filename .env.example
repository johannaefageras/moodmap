# Supabase — fill these in from your project's API settings (https://supabase.com/dashboard)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only — never expose to the client
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI — server-only. Used for AI-genererade sammanfattningar i dashboarden.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
